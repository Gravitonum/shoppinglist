# Shopping List Application

Современное приложение для управления списками покупок с поддержкой совместного доступа, шаблонов и категориз ации товаров.

## Технологический стек

- **Frontend**: React 18 + TypeScript
- **Build Tool**: Vite
- **UI Library**: Ant Design 5.x
- **State Management**: React Query (TanStack Query)
- **Routing**: React Router v6
- **HTTP Client**: Axios
- **Backend**: GraviBase BaaS

## Возможности

- 🔐 Регистрация и вход пользователей с JWT аутентификацией
- 📝 Создание и управление списками покупок
- ✅ Отметка купленных товаров
- 🏷️ Категоризация товаров
- 🏪 Привязка товаров к магазинам
- 📋 Шаблоны для повторяющихся списков
- 👥 Совместный доступ к спискам
- 📱 Адаптивный дизайн для мобильных устройств

## Установка и запуск

### Предварительные требования

- Node.js >= 18.x
- npm >= 9.x

### Установка зависимостей

```bash
npm install
```

### Настройка окружения

Создайте файл `.env` в корне проекта (используйте `.env.example` как шаблон):

```env
VITE_API_BASE_URL=https://shoppinglist.apps.gravibase.ru
VITE_PROJECT_ID=shoppinglist
VITE_AUTH_URL=https://shoppinglist.apps.gravibase.ru/auth/projects/shoppinglist
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Сборка для production

```bash
npm run build
```

### Предпросмотр production сборки

```bash
npm run preview
```

## Структура проекта

```
src/
├── api/              # API клиенты и запросы
│   ├── auth.ts       # Аутентификация
│   ├── client.ts     # Axios клиент с interceptors
│   └── entities.ts   # CRUD операции для сущностей
├── components/       # React компоненты
│   ├── auth/         # Компоненты аутентификации
│   ├── layout/       # Layout компоненты
│   └── shared/       # Общие переиспользуемые компоненты
├── contexts/         # React contexts
│   └── AuthContext.tsx
├── hooks/            # Кастомные хуки
├── pages/            # Страницы приложения
│   ├── auth/         # Страницы входа и регистрации
│   ├── lists/        # Страницы списков покупок
│   ├── categories/   # Управление категориями
│   ├── stores/       # Управление магазинами
│   └── templates/    # Управление шаблонами
├── styles/           # Глобальные стили
├── types/            # TypeScript типы
│   ├── entities.ts   # Типы сущностей
│   └── api.ts        # Типы API
└── utils/            # Утилиты и хелперы
```

## API интеграция

Приложение использует GraviBase Backend-as-a-Service для хранения данных. Все API запросы автоматически включают JWT токен в заголовке Authorization.

### Основные сущности

- **User** - пользователи системы
- **ShoppingList** - списки покупок
- **Item** - товары в списках
- **Category** - категории товаров
- **Store** - магазины
- **ListMember** - участники общих списков
- **TemplateList** - шаблоны списков
- **TemplateItem** - товары в шаблонах

### Аутентификация

- Регистрация: `POST /auth/projects/shoppinglist/users`
- Вход: `POST /auth/projects/shoppinglist/token`
- Обновление токена: `PUT /auth/token`

Токены автоматически обновляются перед истечением срока действия.

## Разработка

### Форматирование кода

Проект использует ESLint для проверки качества кода. Рекомендуется настроить автоформатирование в вашем редакторе.

### Добавление новых страниц

1. Создайте компонент в папке `src/pages/`
2. Добавьте роут в `src/App.tsx`
3. При необходимости добавьте пункт меню в `src/components/layout/MainLayout.tsx`

### Работа с API

Все API запросы должны использовать функции из `src/api/entities.ts`. Для создания нового API:

```typescript
import { createEntityAPI } from '@/api/entities';
import type { YourEntity } from '@/types/entities';

export const yourEntityAPI = createEntityAPI<YourEntity>('EntityName');
```

## Roadmap

- [x] Базовая аутентификация
- [x] CRUD списков покупок
- [x] Просмотр товаров в списке
- [ ] Добавление/редактирование товаров
- [ ] Управление категориями
- [ ] Управление магазинами
- [ ] Шаблоны списков
- [ ] Совместный доступ
- [ ] Поиск и автодополнение
- [ ] История покупок
- [ ] PWA поддержка

## Лицензия

MIT
